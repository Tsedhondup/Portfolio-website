export class ContactComponent {
  // contactContainer = CONTACTS;
  // header = this.contactContainer.querySelector(".contact-content h1");
  // phonEmail = this.contactContainer.querySelector(
  //   ".contact-content .phone-email"
  // );
  // message = this.contactContainer.querySelector(".contact-content .messages");
  // location = this.contactContainer.querySelector(".contact-content .location");
  // socialList = Array.from(
  //   this.contactContainer.querySelectorAll(
  //     ".contact-content .contact-socials ul li"
  //   )
  // );
}
